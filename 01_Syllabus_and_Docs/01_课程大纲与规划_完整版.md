# 《Agentic Protocol Modeling & Simulation Studio》  
# 课程大纲与规划（系统完整版）

> 基于项目内 3 份 MD 文件梳理与整合：  
> - 项目1阅读清单（# Project1_readinglist.md）  
> - 项目1教师简报与L2交付物（Project1_UniswapV2_InstructorBrief_and_L2_Deliverables_v1.7_full.md）  
> - 课程设计对话记录（Mar 2, 2026 Markdown Content）

---

## 一、课程定位与目标

### 1.1 课程名称与副标题

- **课程名称**：Agentic Protocol Modeling & Simulation Studio（代理协议建模与仿真工作室）
- **副标题**：用仿真审计 AI Agent 在复杂协议与跨协议生态中的策略、风险与治理

### 1.2 与《管理建模与仿真》的关系

- **判题器**：只要项目回答的是——**在资源约束下（资本/流动性/额度/算力/权限/时间），如何设计机制或策略，并用仿真比较其在不确定与动态环境中的绩效（收益/效率/风险/稳定性/合规）**——即属于管理建模与仿真。
- 链上协议 = 可编程组织与机制；AI Agent = 新的执行者/决策者。课程重点：**机制设计 + 策略对比 + 风险治理**。

### 1.3 课程“操作系统”

- **节奏**：8 周，每周半天（4 节课），共 32 节课。
- **项目节奏**：每 2 周完成一个**完整项目**（最多 4 周一个项目），共 4 个项目。
- **参与方式**：每人独立完成（超级个体），所有成果上传 GitHub，并提交**可供 AI 自动验证与打分的交付物**（CI 门禁 + grade.json）。
- **课堂形态**：讲解（Instructor Brief）+ 现场演示（Live Build）+ 实做（Studio）+ 分享与问答（Show & Ask## 三、项目 1（第 1–2 周）：Uniswap V3 底层物理法则与协议建模

### 3.1 目标与边界

- **目标**：抛弃 V2 玩具模型，把真实的 Uniswap V3（集中流动性）抽象成**离散分栏的可计算状态机**。用代码复刻流动性穿越 Tick 时的生死断言，并进行交易并发压力测试。
- **不要求**：链上开发、Gas 细节；前期不需要复杂的智能套利与博弈，纯属机制的验证。

### 3.2 系统边界与状态（教师讲解要点）

- **模拟范围**：单币对现货池（支持 Tick 分档）、固定量级的 Swap、精确的手续费留存。
- **核心状态**：  
  - 核心字典：`Ticks`（离散价格篮子，每个包含了自身的资产储备）、`Current_Tick`、`Global_Fee`。
- **状态更新与红线（不变量）**：
  - 必须确保价格未穿越某个 Tick 时，其中的资产处于“死寂”状态不可动用。

### 3.3 验证与测试门禁

- 写出极具摧毁性的断言（Assertions），例如：如果价格连插 10 个 Tick，整个数学池内的资产加上扣留的手续费，在微积分求导计算下必须丝毫不差（允许 1e-12 精度下近似）。
- 需绘制在连续窄幅震荡与单边闪崩图景下的【集中资金利用效率图】与【无常损失滑坡图】。

---

## 四、项目 2（第 3–4 周）：单 Agent 第一代拓荒者——基于 V3 的主动 LP 策略仿真

### 4.1 目标

- 引入第一个完整的决策智能体（Agent）。要求针对上一章的死亡引擎，设计并测试动态调仓策略。证明策略并不是只吃手续费，还能抗打。

### 4.2 讲解要点

- **核心 Trade-Off（权衡）**：由于 V3 放大了资本利用率，同时也百倍放大了无常损失（Impermanent Loss）。
- 极窄区间（高额暴利）VS 频繁调仓（高频割肉/Gas摩擦）。

### 4.3 典型策略要求

- 对比基准：被动持有宽区间（Baseline），与学生自己设计的一种主动侦测突破再平衡的 JIT 极窄拦截策略或布林带动态追踪策略。

### 4.4 必交付与验收

- **必须给出翻车证明图**：在什么震荡走势下，主动调仓的磨损成本（磨损线）会无可挽回地越过收租利润线（利润线），发生惨死。不求全赢，只求知底。

---

## 五、项目 3（第 5–6 周）：跨越深渊——多协议对冲组合策略仿真（Delta Neutrality）

### 5.1 目标

- **维度跃迁**：承认在单点 V3 中无常损失无法消除。要想赚纯粹的手续费利润，必须引入**外部世界（Aave 或 永续合约）**来对冲基础资产池的跌幅敞口，打造风险中性的避风港。

### 5.2 讲解要点

- **系统性风险连环爆破**：教导学生，跨协议借钱对冲不等于无风险白嫖。在抵押物跳水时，这笔资金可能遭受清算借贷池的无底洞拖累。
- 对冲成本与资金利用率之间的数学极值在哪里。

### 5.3 必交付与验收

- 仿真引擎须横向扩展第二个子模块：借贷池。
- 在遭遇“V型反转黑天鹅冲击”剧本下，交出跨协议资产负债表的净值（Net Value）不出现断崖式下跌的证据。

---

## 六、项目 4（第 7–8 周）：黑暗森林——多主体微观市场博弈仿真（Multi-Agent ABM）

### 6.1 目标

- **最终洗礼**：当市场不再是盲盒代码，里面塞满了聪明但自私的掠夺者。将系统从微观单智能体升级为复杂系统下的多主体进化与相互倾轧。

### 6.2 讲解要点

- 引入真正的生物链：噪音盲目交易者（散户/输血者）、毒性套利掠夺者（Arbitrageurs/抽血者）、内卷的其他高频主动 LP。
- 观察并理解纳什均衡的崩溃（当所有人都想占据最优 Tick 时导致的流动性踩踏）。

### 6.3 必交付与验收

- 不光要写出在博弈中存活下来的策略（或承认个人英雄主义的死亡）；
- 更要是站在**协议缔造者（DAO 治理者）**视角，通过仿真输出一份**《治理改革提案》**：若改变动态费率（Dynamic Fee）或引入限速惩罚机制，能否挽救流动性枯竭的公地悲剧？ons, min_reserve_x/y, nan_inf_count, avg_price_mid, fee_rate 等）；`results/figures/` 至少 2 张图（price_mid_timeseries, k_timeseries）。  
- **README**：一页 memo，结论 + 证据指向（metrics/figures/tests）。

### 3.5 阅读与工具（对应阅读清单）

- **协议必读（A）**：Uniswap v2 官方文档（How it works, Swaps, Fees, Pricing, Common Errors）+ 白皮书节选。  
- **验证与可复现（B）**：pytest 入门/断言/fixtures、Python random seed、GitHub Actions 跑 Python 测试。  
- **实验输出（C）**：pandas CSV、matplotlib 基本画图与 savefig。  
- **选读（D）**：Uniswap v2 入门解读、v2-core 仓库结构、Hypothesis、SALib/Mesa（后续项目）。  

**最短路线**：A1–A6 + B7–B11 + C12–C13。

### 3.6 交付物与验收（L2）

- **spec.yaml**：课程/项目/版本、决策问题、scope、state_variables、protocol_model、randomness_and_scenarios、verification_plan、outputs。  
- **experiments/scenarios.yaml**：6 个标准情景（S1–S6）。  
- **tests/test_invariants.py**：I1–I8（至少 I1–I5）。  
- **src/simulator.py** 最小接口：`get_amount_out`, `swap_x_for_y`, `swap_y_for_x`。  
- **CI**：pytest 全绿；`python -m experiments.run_all` 生成 metrics.csv 与 figures。  
- **可选**：grade.json（tests/scenarios/artifacts 等机器可验证字段）。

---

## 四、项目 2（第 3–4 周）：基于单协议的简单策略

### 4.1 目标

- 在**单协议**上做**策略对比**（baseline vs candidate），证明“策略实验”能力，避免只在一个情景上“赢”。

### 4.2 讲解要点

- 策略空间怎么写进 spec（杠杆、阈值、频率、风险上限）。  
- 反过拟合：策略须在情景集合上鲁棒。  
- 敏感性分析最小版：参数扫 + 结论边界。

### 4.3 典型策略题

- AMM：被动持有 vs 再平衡/撤出阈值/动态区间（简化）。  
- 借贷：固定抵押率 vs 动态补仓/止损/清算前预警。  
- 闪电贷可作为“压力情景注入器”（教学用，不要求真实链上交易）。

### 4.4 必交付与验收

- **必交**：`policies.py` 至少 2 个策略；多情景×多种子批量实验；metrics 含收益/成本/风险；README 写明“推荐策略”与“翻车条件”。  
- **自动验收**：candidate 在 ≥X% 情景中不劣于 baseline；尾部风险（最大回撤/破产概率）不显著恶化；输出含 trade-off 图（收益-风险-成本）。  
- **可选加分**：策略成本（gas/滑点）、执行错误率（Agent 误判）入模。

---

## 五、项目 3（第 5–6 周）：跨协议策略

### 5.1 目标

- **跨协议**路由与组合：资源在多个协议/池子间分配；理解耦合与级联风险。

### 5.2 讲解要点

- 组合系统状态与耦合（一协议冲击如何传导到另一协议）。  
- 实验设计：情景注入优于“拟合历史”（尤其两周周期）。  
- 系统韧性指标：最大损失、恢复时间、崩溃概率。

### 5.3 必交付与验收

- **必交**：simulator 支持 ≥2 个协议模块（模块化）；routing_policy（何时迁移、迁移多少）；至少一类“冲击传播”情景；README 回答跨协议收益来源、新增风险、缓解机制。  
- **自动验收**：模块可单独运行；冲击情景类型×强度覆盖；candidate 在冲击下韧性不弱于 baseline（可允许收益略低但风险更低）。  
- **可选加分**：治理成本入模；策略选择地图（不同风险偏好下选不同策略）。

---

## 六、项目 4（第 7–8 周）：多 Agent 博弈

### 6.1 目标

- 把市场/协议生态视为**多主体系统**，策略表现依赖“他人行为”；从“我怎么赚钱”上升到“规则怎么改才能降低系统性风险”。

### 6.2 讲解要点

- ABM 最小骨架：主体状态、规则、交互、事件队列。  
- 博弈用“策略实验”比较：均衡趋向、拥挤效应、踩踏风险。  
- 机制设计视角：费用规则、清算惩罚、限速、抽检审计等。

### 6.3 必交付与验收

- **必交**：至少 3 类 Agent（其中 1 类为对抗/套利/清算）；至少 2 套机制/规则对比；系统级指标：稳定性（崩溃概率/大幅波动）、公平性/集中度、效率（成交量/资金利用率等）。  
- **自动验收**：多种子下统计性质稳定；对抗者在 baseline 下能造成可观影响；机制改造后系统风险改善、效率损失可接受。  
- **可选加分**：AI Agent 不可靠性（误动作/误判概率）；审计预算约束（抽检越多越安全但越贵/越慢）。

---

## 七、统一 Repo 结构与自动验收

### 7.1 标准目录（四个项目一致）

```
project/
  README.md              # 一页决策 memo + 复现说明
  spec.yaml              # 机器可读：决策、KPI、边界、假设、实验计划
  src/
    simulator.py         # 协议/环境仿真
    policies.py          # baseline + candidate（P2 起）
    metrics.py           # 确定性指标计算
  experiments/
    scenarios.yaml
    run_experiments.py   # 一键 -> results/
  results/
    metrics.csv
    figures/
  tests/
    test_invariants.py   # 不变量/边界
    test_reproducibility.py
  grade/
    grade.py             # 生成 grade.json
    rubric.yaml
    grade.json           # CI 产出
  .github/workflows/ci.yml
  requirements.txt
  （可选）Dockerfile、PROMPTLOG.md
```

### 7.2 AI 可自证自评原则

- **分数主干**由确定性程序产生（CI + grade.py → grade.json）。  
- **AI grader**：基于 grade.json 与证据路径，检查 memo 与证据一致性、边界与风险表述，并给出解释性点评；不依赖“主观感觉”打分。

### 7.3 门禁与证据

- **硬门禁**：tests 全绿、可复现（固定 seed）、运行时间上限（如 120s）。  
- **证据生成**：跑情景 → metrics.csv / figures → grade.py → grade.json。

---

## 八、8 周每周半天安排（含讲解、实践、分享与问答）

### 固定节奏（每周 4 节）

1. **Instructor Brief（讲解）**：45–60 min — 本周唯一关键概念 + 常见坑 + 验收标准。  
2. **Live Build（现场演示）**：45–60 min — 从 starter repo 做最小功能（一个不变量测试/一个情景/一张图）。  
3. **Studio（实做）**：60–90 min — 巡回答疑；要求当场 push，CI 跑通。  
4. **Show & Ask（分享+问答）**：30–45 min — 3–5 人闪电分享（坑与验证）；公共 Q&A。

### 八周日程概要

| 周 | 主题 | 讲解重点 | Studio / 交付 |
|----|------|----------|----------------|
| 1 | P1 Kickoff | 课程操作系统、repo 合同、CI、可验收模型 | spec v0、CI 跑通、最小 swap/池子骨架 |
| 2 | P1 Audit & Demo | 常见失败模式、数值与边界 | 红队审计、Demo：scenarios → metrics/figures → memo |
| 3 | P2 Kickoff | 策略空间、反过拟合、敏感性最小版 | 两策略 + 批量实验管线 |
| 4 | P2 Audit & Demo | 鲁棒性门槛、尾部风险、翻车条件 | 红队攻“过拟合”；收益-风险-成本图 |
| 5 | P3 Kickoff | 耦合与级联、模块化、冲击注入 | 第二协议模块 + 跨协议策略 |
| 6 | P3 Audit & Demo | 韧性指标、治理成本 | 冲击情景对比；系统级结论 |
| 7 | P4 Kickoff | ABM 最小骨架、对抗、机制设计 | 3 类 Agent + 2 套机制 |
| 8 | P4 Audit & Final Demo | 机制 trade-off、审计预算 | 红队终审；机制 A vs B 稳定性对比；课程总结 |

---

## 九、八周讲授单元（你“讲什么”的系统化）

1. **建模合同与可验收性** — 什么叫“模型做完”：CI 全绿 + 证据链。  
2. **协议作为动态系统** — 状态、事件、守恒、不变量（以 AMM x*y=k 为例）。  
3. **随机性建模** — 交易流/价格/冲击（情景 > 拟合）。  
4. **策略实验设计** — 避免过拟合、情景集合、对照与消融。  
5. **敏感性与鲁棒性** — 参数扫、尾部风险、破产概率。  
6. **跨系统耦合与级联风险** — 传播、反馈、恢复时间。  
7. **多主体与机制设计** — 拥挤、对抗、规则改造。  
8. **治理与审计** — 权限、抽检、限速、回滚（可计算机制）。

---

## 十、评分体系

- **单项目**：约 22% × 4。  
  - 约 12%：机器硬评分（CI/grade.json：tests、可复现、情景覆盖、指标正确性、鲁棒性阈值）。  
  - 约 6%：证据链与解释（memo 与 metrics/figures 一致、翻车条件明确）。  
  - 约 4%：建模质量（边界、假设、主要矛盾）。  
- **课堂贡献**：约 12% — 有效 push + 红队参与 + 闪电分享（至少 2 次，内容为验证/失败/复现）。

---

## 十一、安全与边界（建议写进 Syllabus）

- 所有实验为**离线仿真/合成或公开数据**；不要求、也不允许真实资金交易或连接真实钱包。  
- 课程产出是**建模与仿真证据链**，不构成投资建议。  
- 评价重点：模型结构、验证、实验设计、鲁棒性、治理机制，而非“赚得最多”。

---

## 十二、教学基建清单（一次搭好，整门课可复用）

1. **4 个 starter repo**：统一目录、CI、grade.py、rubric.yaml、最小可运行骨架。  
2. **统一 spec.yaml schema**：四个项目共用。  
3. **情景/冲击注入器**：便于学生不卡在“找数据”。  
4. **失败模式手册**：每次 Audit 前发放，减少低级错误。  
5. **项目 1 专用**：完整 spec.yaml、scenarios.yaml、test_invariants.py 示例及阅读清单（见第三节与 3.5）。

---

## 十三、三份 MD 文件与本章大纲的对应关系

| 内容维度 | 主要来源 |
|----------|----------|
| 课程定位、8 周结构、四类项目、评分、课堂节奏 | Mar 2, 2026 对话（Markdown Content） |
| P1 详细机制、不变量 I1–I8、情景定义、L2 交付物、spec/scenarios/test 示例 | InstructorBrief_and_L2_Deliverables |
| P1 阅读顺序、协议/验证/输出/选读、最短阅读路线 | # Project1_readinglist |

---

以上为基于项目内 3 份 MD 梳理后的**系统完整课程大纲与规划**，可直接用于 Syllabus、教学日历、starter repo 说明与助教验收标准。若需某一周或某一项目的“单页授课脚本”或“学生版 Brief”，可在此基础上再拆解成独立文档。
